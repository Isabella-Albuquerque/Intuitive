import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  flex: {
    flex: 1
  },
  safeArea: {
    flex: 1,
    backgroundColor: '#fafafa'
  },
  container: {
    flexGrow: 1,
    padding: 24,
    backgroundColor: '#fafafa'
  }
})
